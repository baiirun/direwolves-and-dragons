import React from 'react';
import styled from 'styled-components';

const Character = () => {
    return <Card>I'm the character UI</Card>;
};

const Card = styled.div`
    display: flex;
    flex-direction: column;
`;

export default Character;
